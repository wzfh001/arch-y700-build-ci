# tablet-kde P5 bundle 制作（2026-08-02，Wi-Fi 修复后 run）

> CI run `30736975180`（`codex/tablet-kde-20260802` @ `d1d3c1d`）P4 VERIFIED 后执行。
> 复用已验证 GPT 数值：userdata LBA 3613096..123365370，
> program XML start=3613096/count=5242880/LUN0/4096/sparse=false。
> **2026-08-02 完成**：19 成员全部就位，`sha256sum -c BUNDLE-SHA256SUMS.txt` 19/19 PASS；
> 10 点回读期望从 KDE raw 重算 10/10 MATCH（`EXPECTED-READBACK-SHA256.tsv`）。

## 输入（实际完成）

| 项 | 来源 |
|---|---|
| 主 artifact | `gh run download 30736975180`（3.5 GiB，raw SHA 已验证） |
| rootfs.img.7z / grub.img.7z / boot.img.7z | artifact 内（SHA256SUMS 交叉校验） |
| known-gpt LUN0-5 | 旧 bundle `TB321FU-tablet-niri-run-29966711103/known-gpt/`（复用，实机 GPT 已验证，逐字节一致） |
| firehose XML（program/read-gpts/read-userdata） | 旧 bundle 复用（逐字节一致） |

## 新 run 镜像身份

- rootfs raw `3a472fbca5ac591794ea238d27225029836919f9c23215db16ba562956d78ae2`（21474836480 B）
- grub raw `f54035fd298b6f81ad7a26893b6007e3e7b62a03372899f8b49573be2ba09945`（268435456 B）
- boot raw `a9c2e176b34d3205f8f71b5819c58d215dcb993395191d8d44f37d922f592b02`（100663296 B）
- 标准路径 board-2.bin `c896bc77…`（202148B）——Wi-Fi 固件布局修复已生效

## 步骤

1. `mkdir -p builds/flash-bundles/TB321FU-tablet-kde-run-30736975180/{images,firehose,known-gpt,scripts,readback-expect}`
2. 复制 artifact 内 3 个 7z 到 `images/`，`7z t` 验证
3. 解压 rootfs.img.7z → raw，`e2fsck -fn`（0 errors, 232575 files）+ `sha256sum`
4. 复制旧 bundle 的 `known-gpt/*.bin`、`firehose/*.xml`
5. 按新 raw SHA 重新生成 10 点回读期望（`generate-readback-expectations.sh`），独立脚本重算 10/10 MATCH
6. 写 `ARTIFACT-IDENTITY.txt`（run 30736975180、commit d1d3c1d、Wi-Fi 固件证据）
7. 更新 `Prepare-Images.ps1` 的 `$WorkRoot`（`Y700_Arch_tablet_kde_run_30736975180`）、archives 数组、imageSpecs Path/Size/Sha256
8. 生成 `BUNDLE-SHA256SUMS.txt`（19 成员，无自引用行）
9. `sha256sum -c BUNDLE-SHA256SUMS.txt` 19/19 **PASS**

## 回读验证（Windows QFIL）

- `Verify-Bundle.ps1` → `Prepare-Images.ps1` → QFIL 刷写 → `Verify-Readback.ps1` 10/10
- `Verify-TB321FUGpt.ps1` 确认 6 LUN GPT 与 known-gpt 一致（只读重读，不写）

## 关键红线

- **绝不 fastboot 写 20G userdata**（Fastboot sparse 漏尾 bug 已确认）
- 刷前必须 9008 只读重读 GPT 与 known-gpt 比对
- bundle 成员哈希与 ARTIFACT-IDENTITY 完全一致才可交付
