# 关闭蓝牙网络（BT NAP / BNEP）fragment
# 基准：vendor 基线 CONFIG_BT_BNEP=m + MC_FILTER/PROTO_FILTER=y
# 目标：用户不需要蓝牙共享网络（有 Wi-Fi），移除 BNEP 内核模块面
# 依据：BT_BNEP Kconfig = "required for Bluetooth PAN"；蓝牙键盘走 HIDP，不依赖 BNEP；
#       ALARM 官方虽保留 BNEP=m，但本设备明确不需要蓝牙网络。
# 影响：- CONFIG_BT_BNEP / _MC_FILTER / _PROTO_FILTER 关闭
#       - 保留 CONFIG_BT / BT_BREDR / BT_RFCOMM / BT_HIDP（蓝牙键盘/音频保持可用）
#       - overlay 侧 tb321fu-bt-nap 脚本、service、bt nmconnection、bnep 模块加载已同步移除
# 状态：候选（未上机）；K9 纪律：单组变量、独立 EXP、可回滚基线单元
# CONFIG_BT_BNEP is not set
# CONFIG_BT_BNEP_MC_FILTER is not set
# CONFIG_BT_BNEP_PROTO_FILTER is not set
