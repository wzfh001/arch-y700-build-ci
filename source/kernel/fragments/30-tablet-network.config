# TB321FU 网络特性 fragment：WireGuard + nftables
# 基准：vendor 基线（无 WIREGUARD / NF_TABLES）
# 目标：VPN/内网访问可用；消除 systemd nftables 服务因缺 NF_TABLES 的 failed 状态
# 依据：ALARM linux-aarch64 通用 config
# 状态：候选（未上机）
CONFIG_WIREGUARD=m
CONFIG_NF_TABLES=m
CONFIG_CFS_BANDWIDTH=y
